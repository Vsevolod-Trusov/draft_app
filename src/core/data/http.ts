enum Routes {
  UsersURL = 'users',
  ById = ':id',
}

enum Params {
  Id = 'id',
}

enum HeaderNames {
  RefreshHeader = 'x-refresh-header',
  Authorization = 'Authorization',
}
export { HeaderNames, Params, Routes };
